
input_values = [
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('banana', 0.77)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['banana', 0.77],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('darkturquoise', 0.9)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['darkturquoise', 0.9],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('darkturquoise', 1.0)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['darkturquoise', 1.0],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('greenyellow', 0.96)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['greenyellow', 0.96],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('greenyellow', 1.06)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['greenyellow', 1.06],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('cadetblue', 1.46)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['cadetblue', 1.46],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('antiquewhite', 1.06)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['antiquewhite', 1.06],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('indigo', 0.51)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['indigo', 0.51],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('banana', 1.43)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['banana', 1.43],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('antiquewhite', 1.33)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['antiquewhite', 1.33],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('blue', 1.13)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['blue', 1.13],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('lavender', 0.71)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['lavender', 0.71],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('indianred', 0.55)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['indianred', 0.55],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('khaki', 1.4)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['khaki', 1.4],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('aqua', 1.04)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['aqua', 1.04],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('aliceblue', 1.15)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['aliceblue', 1.15],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('aqua', 0.75)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['aqua', 0.75],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('brick', 0.79)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['brick', 0.79],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('cobalt', 0.73)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['cobalt', 0.73],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('coral', 1.08)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['coral', 1.08],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('cobalt', 1.37)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['cobalt', 1.37],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('beige', 0.89)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['beige', 0.89],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('darkgray', 0.59)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['darkgray', 0.59],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('cyan2', 0.95)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['cyan2', 0.95],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('banana', 0.79)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['banana', 0.79],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('darkslategray', 1.19)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['darkslategray', 1.19],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('coldgrey', 1.13)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['coldgrey', 1.13],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('flesh', 0.55)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['flesh', 0.55],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('blanchedalmond', 0.84)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['blanchedalmond', 0.84],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('crimson', 0.61)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['crimson', 0.61],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('cornflowerblue', 0.65)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['cornflowerblue', 0.65],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('darkslateblue', 0.68)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['darkslateblue', 0.68],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('aquamarine1', 1.1)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['aquamarine1', 1.1],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('crimson', 1.37)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['crimson', 1.37],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('darkturquoise', 1.17)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['darkturquoise', 1.17],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('floralwhite', 1.27)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['floralwhite', 1.27],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('burntsienna', 0.61)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['burntsienna', 0.61],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('blueviolet', 1.26)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['blueviolet', 1.26],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('black', 1.45)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['black', 1.45],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('gainsboro', 1.31)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['gainsboro', 1.31],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('indigo', 0.59)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['indigo', 0.59],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('darkorchid', 1.11)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['darkorchid', 1.11],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('lavenderblush1', 1.35)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['lavenderblush1', 1.35],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('lavenderblush1', 0.55)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['lavenderblush1', 0.55],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('cadetblue', 1.12)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['cadetblue', 1.12],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('goldenrod', 0.95)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['goldenrod', 0.95],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('khaki', 0.63)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['khaki', 0.63],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('brick', 0.9)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['brick', 0.9],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('brick', 1.31)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['brick', 1.31],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Bicicleta',
        # Constructors arguments: list[obj]
        'init_args':[('honeydew1', 1.16)],
        # Names to execute: list[str]
        'names':['color', 'altura'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['honeydew1', 1.16],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    }
]
