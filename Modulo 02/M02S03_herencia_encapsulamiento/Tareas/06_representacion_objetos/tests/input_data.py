
input_values = [
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(66, 22, 'green')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(66, 22, "green")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(20, 13, 'beige')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(20, 13, "beige")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(57, 27, 'darkgoldenrod')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(57, 27, "darkgoldenrod")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(39, 2, 'banana')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(39, 2, "banana")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(80, 15, 'ivoryblack')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(80, 15, "ivoryblack")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(20, 12, 'green')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(20, 12, "green")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(42, 12, 'floralwhite')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(42, 12, "floralwhite")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(68, 10, 'goldenrod')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(68, 10, "goldenrod")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(18, 22, 'honeydew1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(18, 22, "honeydew1")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(31, 17, 'cyan2')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(31, 17, "cyan2")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(49, 3, 'brown')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(49, 3, "brown")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(99, 11, 'burntumber')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(99, 11, "burntumber")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(85, 19, 'khaki')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(85, 19, "khaki")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(65, 8, 'flesh')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(65, 8, "flesh")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(54, 15, 'crimson')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(54, 15, "crimson")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(54, -27, 'goldenrod')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(65, 1, 'flesh')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(65, 1, "flesh")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(13, 5, 'flesh')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(13, 5, "flesh")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(78, 9, 'lavender')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(78, 9, "lavender")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(90, 8, 'azure1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(90, 8, "azure1")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(63, 17, 'dimgray')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(63, 17, "dimgray")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(63, 16, 'chocolate')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(63, 16, "chocolate")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(95, 19, 'dimgray')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(95, 19, "dimgray")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(12, 20, 'darkseagreen')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(12, 20, "darkseagreen")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(56, -20, 'bisque1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(61, 8, 'darkorange')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(61, 8, "darkorange")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(57, 18, 'darkslategray')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(57, 18, "darkslategray")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(27, 18, 'darkkhaki')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(27, 18, "darkkhaki")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(22, 12, 'darkolivegreen')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(22, 12, "darkolivegreen")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(96, 15, 'aqua')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(96, 15, "aqua")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(59, 11, 'deepskyblue1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(59, 11, "deepskyblue1")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(-10, 10, 'darkturquoise')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(21, 13, 'cadmiumyellow')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(21, 13, "cadmiumyellow")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(63, 14, 'ghostwhite')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(63, 14, "ghostwhite")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(26, 17, 'darkolivegreen')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(26, 17, "darkolivegreen")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(13, 6, 'carrot')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(13, 6, "carrot")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(95, 16, 'cobalt')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(95, 16, "cobalt")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(56, 23, 'carrot')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(56, 23, "carrot")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(60, 19, 'cobaltgreen')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(60, 19, "cobaltgreen")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(71, 13, 'darkgoldenrod')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(71, 13, "darkgoldenrod")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(43, 10, 'ghostwhite')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(43, 10, "ghostwhite")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(33, 18, 'firebrick')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(33, 18, "firebrick")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(17, 9, 'deepskyblue1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(17, 9, "deepskyblue1")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(33, 23, 'darksalmon')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(33, 23, "darksalmon")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(19, 8, 'green')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(19, 8, "green")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(39, -4, 'burntumber')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(99, 18, 'blanchedalmond')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(99, 18, "blanchedalmond")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(97, 24, 'dimgray')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(97, 24, "dimgray")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(47, 29, 'cadetblue')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(47, 29, "cadetblue")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(52, 20, 'coral')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__repr__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['Esfera(52, 20, "coral")'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    }
]
