
input_values = [
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(100, 15, 'blanchedalmond')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:100, peso:15, color:blanchedalmond}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(40, 19, 'blanchedalmond')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:40, peso:19, color:blanchedalmond}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(21, 29, 'cornsilk1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:21, peso:29, color:cornsilk1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(45, 5, 'lavenderblush1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:45, peso:5, color:lavenderblush1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(21, 6, 'darkkhaki')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:21, peso:6, color:darkkhaki}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(86, 6, 'burntumber')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:86, peso:6, color:burntumber}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(83, 27, 'aliceblue')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:83, peso:27, color:aliceblue}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(68, 8, 'aquamarine1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:68, peso:8, color:aquamarine1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(59, 15, 'cornsilk1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:59, peso:15, color:cornsilk1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(32, -4, 'forestgreen')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(10, -30, 'burntsienna')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(58, 24, 'gray')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:58, peso:24, color:gray}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(95, 18, 'darkgreen')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:95, peso:18, color:darkgreen}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(50, 20, 'greenyellow')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:50, peso:20, color:greenyellow}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(95, 12, 'blue')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:95, peso:12, color:blue}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(40, 22, 'ivoryblack')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:40, peso:22, color:ivoryblack}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(44, 21, 'deepskyblue1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:44, peso:21, color:deepskyblue1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(60, 30, 'blanchedalmond')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:60, peso:30, color:blanchedalmond}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(100, 10, 'lavenderblush1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:100, peso:10, color:lavenderblush1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(71, 15, 'honeydew1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:71, peso:15, color:honeydew1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(96, 4, 'gold1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:96, peso:4, color:gold1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(96, 9, 'coral')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:96, peso:9, color:coral}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(-98, 6, 'brown')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(52, 22, 'crimson')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:52, peso:22, color:crimson}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(-70, 28, 'black')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(88, 14, 'coldgrey')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:88, peso:14, color:coldgrey}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(90, 20, 'carrot')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:90, peso:20, color:carrot}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(55, 1, 'chocolate')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:55, peso:1, color:chocolate}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(79, 19, 'darkslateblue')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:79, peso:19, color:darkslateblue}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(100, -3, 'indigo')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(13, 4, 'darkviolet')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:13, peso:4, color:darkviolet}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(33, 12, 'indianred')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:33, peso:12, color:indianred}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(98, 13, 'darkgoldenrod')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:98, peso:13, color:darkgoldenrod}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(51, 7, 'lavenderblush1')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:51, peso:7, color:lavenderblush1}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(30, 1, 'eggshell')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:30, peso:1, color:eggshell}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(98, 13, 'black')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:98, peso:13, color:black}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(93, 2, 'darksalmon')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:93, peso:2, color:darksalmon}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(40, 15, 'ghostwhite')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:40, peso:15, color:ghostwhite}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(91, 18, 'brown')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:91, peso:18, color:brown}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(72, 17, 'blanchedalmond')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:72, peso:17, color:blanchedalmond}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(90, 9, 'carrot')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:90, peso:9, color:carrot}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(18, 3, 'firebrick')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:18, peso:3, color:firebrick}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(43, 16, 'banana')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:43, peso:16, color:banana}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(40, -5, 'beige')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(66, 9, 'blanchedalmond')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:66, peso:9, color:blanchedalmond}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(-70, 9, 'burntsienna')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[ValueError],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':[],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(74, 6, 'blanchedalmond')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:74, peso:6, color:blanchedalmond}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(66, 21, 'darkorchid')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:66, peso:21, color:darkorchid}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(75, 18, 'ivoryblack')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:75, peso:18, color:ivoryblack}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },
    {
        # Namespace: str
        'namespace':'Esfera',
        # Constructors arguments: list[obj]
        'init_args':[(98, 9, 'burntumber')],
        # Constructors exceptions: list[Exeption]
        # If an exception is expected, only the first exeption must
        # be specifies, all previous instantiations must be None
        'constructors_except':[],
        # Names to execute: list[str]
        'names':['__str__'],
        # Names inputs, in the same order as names: list[str]
        'names_inputs':[],
        # Expected results in the same order as names: list[obj]
        # If the expected result is an exception, specify the name
        # the exception.
        'expected_results':['{radio:98, peso:9, color:burntumber}'],
        # Standard outputs via print, input, etc: list[str]
        'std_outputs':[],
        # Prefix error message
        'error_msg':''
    },

]
